from setuptools import setup, find_packages

setup(
    name='FlowVeloTool',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # List dependencies here if known, e.g., 'numpy', 'matplotlib'
    ],
    author='Anette Eltner',
    description='A tool for processing and analyzing flow velocity fields.',
    url='https://github.com/AnetteEltner/FlowVeloTool',
)
